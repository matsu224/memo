# Unity & C# 備忘録

## 1. Unity Editor の基本

### 主要ウィンドウの役割
* **Projectウィンドウ (パソコンのフォルダ/倉庫)**
  * プロジェクト内のすべての素材（画像、音、3Dモデル、スクリプトなど）を保管する場所。ここにあるのは単なる「ファイル」であり、フォルダの中にファイルを入れることはできても、ゲームオブジェクト同士の「親と子」のような階層構造を作ることはできない。
* **Hierarchyウィンドウ (設計図の階層/親子関係)**
  * 現在のシーン（画面）に配置されているオブジェクト（実体）を管理する場所。オブジェクトをドラッグ＆ドロップして「親・子・孫」といった階層構造（座標や回転の連動など）を作るための場所。
* **Inspectorウィンドウ (詳細設定パネル)**
  * Hierarchyや Project で選択した「1つのオブジェクト（またはアセット）」の詳細情報を表示・編集する場所。GameObjectにアタッチされている `Component`（Transform, Rigidbody, スクリプトなど）が縦に並び、そこで数値の変更やコンポーネントの追加・削除を行う。
* **Sceneビュー (制作用の3D/2D空間)**
  * 開発者がオブジェクトを配置・編集するための作業空間。ここでの見え方は「実際のカメラ」ではなく、あくまで編集用の自由視点である。
* **Gameビュー (プレイヤー視点のプレビュー)**
  * シーン内のカメラ（Main Cameraなど）を通して実際にプレイヤーが見る映像をプレビューする画面。再生ボタン（▶）を押すとここでゲームが実行される。
* **Consoleウィンドウ (ログとエラーの確認)**
  * `Debug.Log()` で出力したメッセージや、スクリプトのエラー・警告（赤色のError、黄色のWarning）が表示される場所。エラーメッセージをダブルクリックすると、原因のスクリプトの該当行にジャンプできるため、不具合の調査に必須のウィンドウ。
* **Profilerウィンドウ (パフォーマンスのレントゲン)**
  * ゲームの処理負荷（CPU、GPU、メモリ使用量など）をリアルタイムで可視化・分析するツール。
  * 「ゲームがカクつく」「メモリを使いすぎている」といった問題が発生した際、どのスクリプトのどの処理（Update関数内の重いループや、文字列結合によるGC Allocなど）が原因でボトルネックになっているかをピンポイントで特定するために使用する。
* **ツールバー (Move / Rotate / Scale / Rect Transform ツール)**
  * Sceneビュー上部のアイコン群。`W`(移動)・`E`(回転)・`R`(拡縮)・`T`(Rect Transform)・`Q`(視点移動/手のひらツール)のショートカットキーで素早く切り替えられる。中央の再生関連ボタン（▶ Play / ⏸ Pause / ⏭ Step）でプレイモードの制御を行う。

### タグ (Tag) とメタファイル (.meta)
* **Unityのタグ (Tag) とは**
  * オブジェクトをグループ分けするための名札機能。「Player」や「Enemy」といったタグを設定することで、スクリプトから特定の種類のオブジェクトだけを検索したり、当たり判定の相手を識別したりする際に使用する。
* **チーム開発で絶対やってはいけない「.metaファイルの無視」**
  * Unityは素材（アセット）を取り込む際、不可視の `.meta` ファイルを自動生成し、そこにIDやインポート設定を記録している。Gitなどのバージョン管理でこの `.meta` ファイルの共有を漏らしたり勝手に削除したりすると、シーン上のプレハブやスクリプトの参照（紐づけ）がすべて切れてプロジェクトが壊れるため厳重注意が必要。

### エディタのレイアウトと設定
* **推奨レイアウト (2 by 3)**
  * Sceneビュー（開発画面）とGameビュー（プレイヤー視点）を同時に確認できるため、配置やカメラの調整結果をリアルタイムに確認しやすい。
* **Projectウィンドウの視認性向上**
  * デフォルトの表示形式より、ウィンドウ右上の `⋮` (More) メニューから「One Column Layout」を選択すると階層が見やすくなる。
* **Sceneギズモの有効化**
  * オブジェクトを正確に配置・移動・回転させるため、Sceneビュー右上の「Orientation gizmo（方向ギズモ）」は常に有効にしておく。
* **Playmode Tint (プレイモード時の色変更)**
  * `Edit > Preferences > Colors > Playmode tint` を設定することで、プレイモード中のエディタの背景色を変更できる。プレイモード中であることを視覚的に分かりやすくし、誤って編集してしまう（後でリセットされる）ミスを防ぐために重要。

### プロジェクトとシーンの管理
* **プロジェクトのクリーンアップ**
  * プロジェクト作成時に自動生成される不要なファイルや、使わない SampleScene は削除し、プロジェクト内を整理整頓しておく。
* **シーン専用フォルダの作成**
  * `Assets` フォルダ直下に `Scenes` フォルダを作成し、シーンファイル（`.unity`）はそこにまとめて管理するのが標準的な運用。
* **こまめな保存 (Ctrl+S / Mac: Cmd+S)**
  * 未保存の変更があるシーンは、Hierarchyウィンドウのシーン名の横にアスタリスク `*` が表示される。データを失わないよう定期的に保存する癖をつける。

### 【重要】 プレイモード中の変更保存ルール
* **Hierarchy上の変更:** プレイモード中にHierarchy（画面上の実体）の数値を変更しても、プレイモードを終了すると**すべて元に戻る（リセットされる）**。調整に便利だが、良い値が見つかったらメモしておく必要がある。
* **Projectウィンドウ（アセット）の変更:** プレイモード中であっても、Projectウィンドウ内の大元のデータ（Prefabファイル、マテリアル、スクリプトなど）を直接編集した場合、**即座に永久保存され、プレイモード終了後も元に戻らない**。
* **値のコピー＆ペーストによる反映:** プレイモード中に見つけた良い設定値は、コンポーネント右上の `⋮` メニューから `Copy Component` を実行し、プレイモードを終了した直後に `Paste Component Values` を実行することで、簡単に本番データへ反映できる。

### よくある初心者向けの躓きポイント
* **【重要】 NullReferenceException（ぬるぽ）**
  * 「変数に何のオブジェクトも入っていない（参照先が存在しない）」状態のまま、そのオブジェクトを使おうとすると発生する最頻出のエラー。原因の多くは、Inspector上でオブジェクトのドラッグ＆ドロップ（アサイン）を忘れている、または `GetComponent` で取得したいコンポーネントがそのオブジェクトに付いていないことにある。
* **MissingComponentException**
  * スクリプト内で `GetComponent<T>()` を使って取得しようとしたコンポーネントが、対象のGameObjectに実際にはアタッチされていない場合に発生するエラー。「そのコンポーネントは本当についているか」をInspectorで確認するのが第一の対処法。
* **Consoleウィンドウを常に確認する習慣**
  * 動作がおかしいと感じたら、まずConsoleウィンドウにエラーや警告が出ていないかを確認する。エラーメッセージをダブルクリックすれば該当のスクリプト行に飛べるため、原因特定の最短ルートになる。

## 2. GameObject と Transform の基本操作

### 配置と初期化
* **【重要】 配置直後のTransformリセット**
  * 空のオブジェクトやプレハブをHierarchyに追加した直後は、Inspectorから `Transform` の `Position` を `(0, 0, 0)` にリセットする。これによりワールド空間の原点が基準となり、後続の配置や計算時のバグを防ぐことができる。
* **Empty GameObject (空のオブジェクト) の活用**
  * Transformコンポーネントのみを持つオブジェクト。画面上には見えないが、複数のオブジェクトをまとめる「コンテナ（親オブジェクト）」として階層構造を整理するのによく使われる。
* **Create Empty (空のオブジェクト) の生成位置**
  * Hierarchyで何も選択せずに右クリックから `Create Empty` を実行した際、オブジェクトは「空間の原点 (X=0, Y=0, Z=0)」ではなく「**現在のSceneビューで見ている画面のど真ん中**」の空中に生成される。
  * ※特定のオブジェクトを右クリックして `Create Empty` を選んだ場合は、そのオブジェクトの子要素として生成される。

### 親子関係の操作
* **親オブジェクトと子オブジェクトの選択**
  * プレハブや複数のパーツで構成されたオブジェクトを移動・編集する際は、必ず「親オブジェクト全体」を選択して操作する。誤って子オブジェクトだけを動かすと、相対座標がずれて崩れてしまうため注意。
* **Create Empty Parent (空の親オブジェクトの作成) の挙動**
  * 複数のオブジェクトを選択して「Create Empty Parent」を実行すると、選択したオブジェクト群の中心座標に新しい親オブジェクトが自動作成される。
  * この際、子になったオブジェクトの**ゲーム内での見た目の位置（ワールド座標）は一切変わらない**が、Inspectorに表示されるTransformの値（ローカル座標）は「新しい親からの相対的な距離」に再計算されるため、数値上は変化する。
* **子オブジェクトへのLayer一括適用**
  * 親オブジェクトのLayerを変更した際、子オブジェクトにも同じLayerを適用するか聞かれるダイアログ (`Yes, change children`) が表示される。当たり判定や物理演算の対象を正しく機能させるためには、階層全体にまとめて適用することが一般的。

### 複製・回転・選択の操作
* **オブジェクトの複製 (Ctrl+D / Cmd+D)**
  * Hierarchy上でオブジェクトを選択し `Ctrl+D` (Mac: `Cmd+D`) で複製できる。複製したオブジェクトは元のオブジェクトと全く同じ位置に配置される。
* **スムーズな回転操作**
  * ツールを使ってオブジェクトを回転させる際、ハンドルを直線的にドラッグする (Drag in a straight line) ことで、よりスムーズで細やかな回転調整が可能になる。
* **Ctrl / Cmd キーによる個別選択**
  * Hierarchy上で `Ctrl` (Mac: `Cmd`) キーを押しながらクリックすると、離れた場所にある複数のGameObjectを個別に追加・解除して選択（マルチセレクト）できる。
* **Shift キーによる範囲選択**
  * `Shift` キーを押しながらクリックすると、最初に選択したオブジェクトから最後にクリックしたオブジェクトまでの「間にあるすべてのオブジェクト」を一括でまとめて選択できる。

### 座標軸の基準 (Local と Global)
* Sceneビュー上部にあるトグルボタンで、移動・回転ツールの軸の基準を切り替えられる。
  * **Global (グローバル):** ワールド空間全体の絶対的な基準軸（常にYが上、Zが奥）。
  * **Local (ローカル):** そのオブジェクト自身の向きを基準とした軸（オブジェクトが上を向いていればY軸も上を向く）。オブジェクトの向きに合わせて先端位置などを微調整する場合は、必ず `Local` に設定してZ軸（青い矢印）を動かす。
* **ローカル座標系の正面 (青い矢印)**
  * 移動・回転ツールを `Local` に切り替えた際、表示される **青い矢印 (Z軸)** は常にそのオブジェクトにとっての「正面 (Forward)」を指している。オブジェクトの初期位置がどちらを向いているかを確認・調整する際の絶対的な指標となる。

### 有効化・無効化
* **Is Active チェックボックス**
  * Inspectorウィンドウ最上部（オブジェクト名の左隣）にあるチェックボックス。これを外すことで、オブジェクトをシーンから削除せずに「一時的に無効化」できる。
  * 実行中のスクリプトや表示、物理演算などがすべてストップするため、特定のオブジェクトだけをオフにしてテスト環境を単純化したい場合などに多用される。
* **gameObject.SetActive(false) による無効化**
  * シーンから完全に削除 (`Destroy`) するのではなく、一時的に非表示（無効化）にするメソッド。後で同じオブジェクトを再利用することで、処理負荷を軽減する（オブジェクトプール等）場合によく使われる手法。

### Visual Tag (Sceneビュー用のカラーアイコン)
* 見えない空のGameObject（目印など）に対し、Inspector左上のアイコンメニューからカラータグ（名前付きアイコン）を設定できる。ゲーム画面（Gameビュー）には影響を与えず、Sceneビュー上での位置確認や管理が格段にやりやすくなる。

## 3. Prefab (プレハブ)

### 作成と Instance
* **ドラッグ＆ドロップによる自動プレハブ化**
  * Hierarchy上にあるGameObjectを、Projectウィンドウ（例えば Prefabs フォルダ）にドラッグ＆ドロップするだけで、そのオブジェクトの設計図として自動的に「Prefab化」される。
  * Prefab化されるとHierarchy上のアイコンが青いキューブに変わり、以降はProject内のPrefabを更新すれば、シーン上のすべての同じオブジェクトに変更が適用されるようになる。
* **Prefab (金型) と Instance (実体)**
  * Projectウィンドウにある大元のPrefabは「金型」であり、それをHierarchyにドラッグして配置したものは「Instance (実体/クローン)」と呼ばれる。実体側の数値を変更しても金型には影響しないが、金型を変更するとすべての実体に一斉に反映される。

### Prefab 編集モード
* ProjectウィンドウにあるPrefabをダブルクリックすると、そのPrefab専用の編集画面（Prefab編集モード）に入る。シーン全体ではなく、対象のオブジェクト単体の中身（階層構造やコンポーネント）を安全に独立して編集・確認できる。

### Prefab Variant と Override
* **Prefab Variant とは**
  * 既存のプレハブ（ベース）の性質を受け継いだ「派生プレハブ」。ベースの変更（共通スクリプトの更新など）はすべてのバリアントに自動反映されるが、バリアント側で独自に変更を加える（パラメータやモデルの差し替えなど）ことも可能。共通の動作を持ちながらステータスや見た目が異なるオブジェクトの量産などに非常に役立つ。
* **Prefab Override (プレハブのオーバーライドと青い縦線)**
  * バリアントやシーン上のプレハブインスタンスにおいて、ベースプレハブから意図的に数値を変更（上書き）したプロパティの左側には「青い縦線」が表示される。これを Prefab Override と呼び、オリジナルからどこが独自に変更されているかを視覚的に確認できる。
* **Apply All (オーバーライドの適用範囲)**
  * Inspectorの `Overrides` から `Apply All` を実行した場合、変更が保存されるのは「そのオブジェクトが直接リンクしている（大元になっている）プレハブ1つだけ」である。
  * つまり「派生プレハブ（Variant）」を編集して Apply All を押した場合、更新されるのはその派生プレハブのみであり、さらに大元の「ベースプレハブ」までは遡って適用されない。

## 4. Camera (カメラ) の操作と設定

* **Align With View (視点の同期)**
  * Sceneビューでカメラの構図（見え方）を決定した後、HierarchyでMain Cameraを選択し `GameObject > Align With View`（ショートカット: `Ctrl+Shift+F` / Mac: `Cmd+Shift+F`）を実行すると、Sceneビューの視点がそのまま実際のカメラ位置に反映される。
* **カメラの投影方式 (Projection) の違い**
  * **Perspective (透視投影):** 人間の目と同じように遠近感（奥行き）がある。一般的な3Dゲーム向け。
  * **Orthographic (平行投影):** 遠近感がなく、距離に関わらずオブジェクトが同じ大きさで表示される。2Dゲームやクォータービュー（アイソメトリック）のゲームでよく使われる。表示範囲は `Size` プロパティで調整する。
* **カメラのクリッピング（見切れ）とローカルZ軸の調整**
  * カメラと対象物の間に地形などが挟まり、画面が不自然に切れてしまう（Near Clipに引っかかる）場合、カメラ自身の `Local` のZ軸（青い矢印の逆方向）へ下げる調整を行う。
  * ローカルのZ軸は「カメラの視線（奥行き）そのもの」であるため、ここを動かせばカメラの角度（見え方）を変えずに純粋に対象から距離だけを取ることができ、見切れを解消できる。
* **Background Type (背景の種類)**
  * カメラがズームアウトした際など、ゲームステージの範囲外が見えてしまう場合は、Main Camera の `Environment > Background Type` を調整する。
* **SkyboxからSolid Colorへの変更**
  * デフォルトの `Skybox`（空の表現）から `Solid Color`（単色塗りつぶし）に変更し、シーンの雰囲気に合わせた色をスポイトで指定することで、見切れ部分の不自然さを解消できる。
* **カメラの制御機構 (CameraRig パターン)**
  * 空のオブジェクト (Empty GameObject) を作成し、原点にリセットした上で「CameraRig（カメラ制御用の親コンテナ）」として機能させる手法がよく使われる。
  * Main Camera を CameraRig の子オブジェクト (Child GameObject) に設定することで、スクリプトから CameraRig を動かした際、それに追従してカメラも動くようになる。移動とズーム（Size）の制御を分離でき、管理しやすくなる。

## 5. ライティングの基本

* **Directional Light (平行光源)**
  * 太陽光や月光をシミュレートし、シーン全体の雰囲気と影を決定する。
  * `Color` (色) と `Intensity` (明るさ) を変更することで、夕暮れや月夜などの時間帯を表現できる。
  * `Shadow Type` で影の品質を調整する。`No Shadows` (影なし)、`Hard Shadows` (カートゥーン調のくっきりした影)、`Soft Shadows` (より自然でリアルな影。デフォルトで推奨されることが多い)。
* **Environment Lighting (環境光)**
  * `Window > Rendering > Lighting` の Environment タブから設定。シーン全体の明るさ、コントラストを定義する。
  * `Environment Lighting Source` を `Color` に設定し、テーマ（月夜、ジャングル、砂漠など）に合った色を指定することで、空間のトーンが統一され、視認性とビジュアルの魅力が向上する。

## 6. 物理演算とコリジョン

### Rigidbody と Collider
* **Rigidbody コンポーネント**
  * オブジェクトに重力や物理演算を適用するためのコンポーネント。
  * **Constraints (制約):** 意図しない動きを防ぐために特定の軸の移動・回転を固定できる。例えば、乗り物が横転しないように `Freeze Rotation X/Z` を有効にしたり、空中に浮かないように `Freeze Position Y` を有効にしたりする。
* **Collider コンポーネント**
  * オブジェクトの「当たり判定（物理的な境界）」を定義する。これがないと他のオブジェクトをすり抜けてしまう。
  * **パフォーマンスの最適化:** 複雑な形状の `Mesh Collider` よりも、`Box Collider` や `Sphere Collider` などの単純な形状（プリミティブ）を使う方が計算負荷が低く、パフォーマンス面で有利。形状の大まかな近似で十分なことが多い。
* **生成位置とColliderの干渉に注意**
  * 弾や発射物などを生成する空のオブジェクト（発生源）は、本体の `Collider`（当たり判定）の外（少し前方の空中など）に配置すること。Colliderの内部に配置してしまうと、生成した瞬間に自分自身と衝突して意図しない挙動（自爆など）を起こす。

### Layer (レイヤー)
* **Layer (レイヤー) とは**
  * シーン内のオブジェクトを特定のグループに分類するための機能。主に「特定のグループ間でのみ物理的な衝突判定や影響（爆発の吹き飛ばしなど）を有効にする」や、「特定のグループだけをカメラに映す/映さない」といった高度な制御を行うために不可欠な設定。

### NavMesh (ナビゲーションメッシュ) と AI の移動
* **NavMesh とは**
  * AIキャラクターがシーン内で「移動可能なエリア（歩行可能領域）」と「障害物」を認識し、自動的に経路探索 (Pathfinding) を行うためのシステム。
* **Agent Type の設定と自作**
  * NavMeshをベイク（生成）する際、AIの大きさ（半径や高さ、登れる段差など）を定義した「Agent Type」を指定する。デフォルトの Humanoid だけでなく、Navigation設定画面から対象の形状に合わせて独自の Agent Type を新しく作成・調整することができる。
* **Bake時に特定のLayer (Playersなど) を除外する理由**
  * NavMeshのBake（焼き込み）は、地形などの「動かない固定の障害物」を計算する処理である。もしプレイヤーを含めてBakeしてしまうと、ゲーム開始時のプレイヤーの初期位置が「見えない壁（永久的な障害物）」としてNavMeshに固定されてしまい、AIがそこを通れなくなってしまうため除外する必要がある。

## 7. オーディオ (Audio)

### Audio Source
* 音を鳴らすためのスピーカーの役割を果たすコンポーネント。エンジン音などの環境音は `Loop` にチェックを入れてループ再生させる。
* スクリプトから `Pitch`（音の高さ）をランダムにわずかに変動させる（`Random.Range`）ことで、機械的なループ音に自然な揺らぎ（リアリティ）を持たせることができる。

### Audio Mixer による音声の一括管理
* **Audio Mixer とは**
  * プロジェクト内のすべての音声をカテゴリー（BGM、エンジン音、SEなど）ごとにグループ分けし、音量やエフェクトをマスター単位で一括管理・調整するための強力なツール。
* **出力先 (Output) のルーティング**
  * 音を鳴らす大元の `Audio Source` コンポーネントにある `Output` プロパティに対し、作成したAudio Mixerの特定のグループ（例: Music, SFX）を割り当てることで、その音がミキサーを経由して鳴るようになる。

### 音量の単位 (dB) とスライダーの概念
* **デシベル (dB) と Attenuation (減衰)**
  * Unityのミキサーにおける音量スライダーは「デシベル (dB)」で計測される。
  * `0 dB` が音源の元の音量（最大）であり、スライダーを下げるとマイナスの値になっていく（Attenuation＝減衰）。
  * 下限（通常 -80 dB付近）に達すると実質的に無音となる。一般的にBGMは -30 dB ～ -20 dB、SE（効果音）は -10 dB ～ -5 dB 程度を目標にミキシングするとバランスが良くなる。

### Audio Ducking (ダッキング効果)
* **ダッキング (Ducking) とは**
  * 特定の重要な音（爆発音やキャラクターの台詞など）が鳴った瞬間に、自動的に他の音（BGMなど）の音量を下げ、重要な音を聞き取りやすくするオーディオ演出の基本テクニック。
* **ミキサーでの実装方法**
  * **ターゲット側 (BGMなど):** 音量を下げたいグループに `Duck Volume` エフェクトを追加する（音を受信する側）。
  * **トリガー側 (SEなど):** きっかけとなるグループに `Send` エフェクトを追加し、Receiveプロパティにターゲット側の Duck Volume を指定する（音を送信する側）。
* **パラメータの調整**
  * `Threshold` (閾値) でエフェクトが発動する音量レベルを決め、`Ratio` でどれだけ音量を下げるかを設定する。`Attack Time` と `Release Time` を 0 に近づけると、SEが鳴った瞬間に瞬時にBGMが下がり、鳴り終わると瞬時に元に戻るメリハリのある動作になる。

## 8. UI (ユーザーインターフェース) と Canvas

### Canvas と EventSystem
* **UIとCanvasの関係**
  * UnityでUI要素（Slider、Textなど）を作成すると、自動的に `Canvas` という親オブジェクトが生成される。すべてのUI要素は、必ずこの Canvas の子オブジェクトとして配置される必要がある。
* **Canvas と EventSystem の自動生成**
  * Unityで最初にUI（TextやButtonなど）を作成すると、UIを描画するための `Canvas` とともに、必ず `EventSystem` というオブジェクトが自動生成される。
  * `EventSystem` はマウスのクリックや画面のタップといった「プレイヤーの入力イベント」をUIに伝える心臓部であり、これを誤って削除するとボタンが一切反応しなくなる。

### Render Mode (描画方式)
* **UIの位置が画面に正しく表示される理由 (Screen Space - Overlay)**
  * メニュー画面などのUIは、ワールド空間（3Dのゲーム世界）のY座標を大きく設定して空中に配置したとしても、ゲーム画面には常に正しく最前面に表示される。
  * これはCanvasの標準設定 (Render Mode) が `Screen Space - Overlay` になっているため。3D空間上のどこに配置されていようと、UIは「カメラのレンズの表面（スクリーンの最前面）」に直接張り付くように独立して描画される仕様になっている。作業中のSceneビューを整理するために空中に逃がしているに過ぎない。
* **World Space UI (3D空間へのUI配置)**
  * Render Mode を `World Space` に変更すると、UIを3D空間内の1つの実体（頭上のHPバーやチャージゲージなど）として配置できるようになり、ゲームの世界に直接組み込める。

### Rect Transform と配置
* **Rect Transform (Left, Right, Top, Bottom)**
  * 2DのUI要素の位置や大きさを制御する専用のコンポーネント。
  * `Top`, `Bottom`, `Left`, `Right` などのプロパティは、「親の指定範囲（アンカー）を基準として、内側に向かってどれくらい余白（オフセット）を取るか」を意味する。数値を `0` にすると、親の端にぴったりくっつくように端まで広がる。
* **親オブジェクトへの依存 (Hierarchy placement)**
  * UI要素をシーンに追加する際、どの `Canvas` や親オブジェクトの「子」として配置するかが非常に重要になる。
  * UIの位置やサイズ (`Rect Transform`) は「親オブジェクトのサイズやアンカー」を基準に相対的に計算されるため、指定された正しい階層に配置しないと、レイアウトが大きく崩れたり画面外に消えたりしてしまう。

### マルチ解像度対応 (Canvas Scaler)
* **Scale With Screen Size**
  * 画面解像度が異なる端末（フルHD、4K、モバイル機器など）でプレイした際、UIが小さくなりすぎるのを防ぐ設定。
  * `Canvas Scaler` コンポーネントの `UI Scale Mode` を `Scale With Screen Size` に設定し、基準解像度 (Reference Resolution) を指定することで、画面サイズに応じてUIが自動的に比例拡大・縮小される。

### Slider (スライダー) のカスタマイズ
* **ゲージとしての利用**
  * スライダーをプレイヤーが操作するUIではなく、「状態を示すゲージ」として使う場合、Sliderコンポーネントの `Interactable` (操作可能) のチェックを外す。
  * `Direction` を `Bottom To Top` (下から上へ) に設定することで、値が溜まっていく視覚効果を作れる。
  * ※ `Min Value` (最小値) と `Max Value` (最大値) を設定する際、**必ず先に Max Value を設定してから Min Value を設定する**（Minが現在のMaxを上回ると自動で0にリセットされてしまう仕様のため）。
* **放射状 (Radial) のUIゲージ**
  * UIスライダーは直線的な動きだけでなく、画像設定を応用することで、中心から円形に増減する360度のゲージとしても表現できる。

### 画面遷移とイベント (OnClick と SetActive)
* **OnClick () イベント**
  * `Button` コンポーネントの `On Click ()` リストを使用すると、スクリプトを書かずにInspector上の操作だけで「ボタンを押した時の挙動」を設定できる。
* **SetActive(bool) による画面切り替え**
  * OnClickイベントにUIのCanvas自身などを割り当て、`GameObject.SetActive (bool)` を呼び出すことで画面遷移をノーコードで実装できる。
  * 片方のCanvasのSetActiveのチェックを外し (False = 非表示)、もう片方のCanvasのSetActiveのチェックを入れる (True = 表示) ことで、画面から画面への遷移が成立する。

## 9. Particle System (パーティクルシステム)

* **エフェクトの調整とプレビュー**
  * 爆発や炎などを表現するコンポーネント。`Start Speed` (初速)、`Start Size` (初期サイズ)、`Color over Lifetime` (時間経過による色の変化) などのプロパティを調整して見た目を作り込む。
  * 調整中は、Sceneビュー上に表示されるParticlesオーバーレイの `Restart` をクリックすることで、ゲームを再生しなくてもエディタ上で即座にエフェクトの動きを確認できる。

## 10. Inspector とコンポーネントの整理

* **コンポーネントの並べ替えと整理**
  * Inspector内のコンポーネントは、ドラッグ操作や右上の `⋮` (More) メニューの `Move Up` / `Move Down` で並べ替えが可能。変更が頻繁なスクリプトを下に配置したり、物理演算系 (Rigidbody, Collider) をまとめたりすると作業効率が上がる。
  * ※ `Transform` コンポーネントは全オブジェクトの必須項目であるため、常に一番上に固定される。
* **Inspectorの折りたたみによる作業効率化**
  * Inspector内の各コンポーネントの `▼` (折りたたみ) を閉じて画面を整理することで、スクリプトへのオブジェクトのドラッグ＆ドロップ（紐づけ）作業がスクロールなしでスムーズに行える。
* **スクリプトへのアサイン**
  * スクリプト内で設定された公開変数に対し、Prefab、Hierarchy上のオブジェクト、コンポーネント（Audio Sourceなど）を正しくドラッグ＆ドロップして紐づけることで、初めてシステムとして機能する。

## 11. Sceneビューの便利な操作

* **Fキー (Frame Selected) とダブルクリック**
  * Hierarchyでオブジェクトを選択し、Sceneビュー上で `F` キーを押す（またはHierarchyでダブルクリックする）と、そのオブジェクトが画面の中央に収まるようにカメラが自動でズーム・移動する。正確な配置作業に必須のショートカット。
* **フライスルー操作**
  * Sceneビュー上で「右クリックを押しながら WASD キー」を入力すると、FPSゲームのように空間を自由に移動できる。
* **オービット (視点の旋回)**
  * Sceneビュー上で `Alt` (Mac: `Option`) キーを押しながらドラッグすると、選択しているオブジェクトを中心にカメラが旋回（オービット）する。3D空間で細かい位置合わせを行う際に非常に便利。
* **Sceneギズモを使った視点切り替え**
  * Sceneビュー右上のギズモの各軸（円錐形の矢印）をクリックすると、真上や真横からの視点に切り替わる。
  * **キューブ（中央）のクリック:** パース（遠近感）のある `Perspective` と、遠近感のない `Orthographic`（平行投影）を切り替えられる。当たり判定の微調整などは Orthographic の方が正確に行える。
  * **Freeモードへの復帰:** ギズモを右クリックして `Free` を選ぶと、通常の自由な視点操作に戻る。

## 12. ゲームデザイン・システム設計パターン

* **Game Manager パターン**
  * ゲーム全体の流れ（ラウンドの開始・進行・終了、カメラの制御、勝敗判定、オブジェクトの動的生成など）を統括する専用のオブジェクトおよびスクリプト。特定のオブジェクトに依存しないシステム全体のロジックを集約する設計パターン。
* **Spawner (生成管理) パターン**
  * アイテム（パワーアップ）や敵を出現させる際、「Spawner（スポナー / 生成器）」と呼ばれる専用のスクリプトを持たせたオブジェクトをマップ上に配置する手法が一般的。
  * 候補リストからのランダム選出、出現位置の固定、アイテムが取得されてから再出現するまでの待機時間 (`Respawn Cooldown`) などを Spawner 側で独立して管理することで、ゲームバランスの調整やレベルデザインが非常にやりやすくなる。
* **色補間によるステータス表現 (Color.Lerp)**
  * 2つの色と割合 (0.0～1.0) を渡すことで、その中間の色を計算して返すメソッド。HPなどの割合に応じて「緑 (Full) から赤 (Zero)」へ滑らかに色を変化させるようなUI演出で非常によく使われる。
* **Quit (ゲーム終了) 処理の仕様**
  * ゲームを終了する処理（スクリプトからの `Application.Quit()` 呼び出しなど）は、PC向けなどにビルド（書き出し）された実際のゲームアプリでは正常に機能する。
  * しかし、**Unityエディターでのテストプレイ中**や、**WebGL（ブラウザ向けゲーム）**では仕様上この終了コマンドは動作しない（または無視される）。Quitボタンなどを実装する際は、プラットフォームごとの挙動の違いを考慮しておく必要がある。

## 13. C# Scripting (スクリプトの基本と文法)

### 1. Unityスクリプトの基本構造 (スクリプト ＝ クラス)
* **クラスの構成要素（用語の整理）:**
  * **フィールド (Fields):** クラスが持つ変数（C++の「メンバ変数」に相当）。
  * **プロパティ (Properties):** `get` / `set` を使い、メソッドを変数のように扱えるようにしたもの（例: `Transform.position`）。
  * **メソッド (Methods):** 関数・処理（C++の「メンバ関数」に相当）。
  * **コンポーネント (Component):** Unity特有の呼び方。`MonoBehaviour` を継承し、GameObjectにアタッチできるクラスのこと。
* **スクリプトファイル ＝ 型（クラス）**
  * Unityにおいて、作成した `.cs` ファイル（スクリプト）は単なる処理の記述ではなく、それ自体が独自の「クラス（型）」の定義になる。
  * 例えば `PlayerController.cs` を作成すると、自動的に `PlayerController` という新しい型（クラス）がゲーム内に誕生する。
* **アタッチ ＝ コンポーネント（型）の付与**
  * Unityで作成したクラスは、基本的に `MonoBehaviour`（モノビヘイビア）という大元のクラスを継承する。
  * これを継承することで初めて、GameObjectに「コンポーネント（部品）」としてくっつけることが可能になる。エディタ上でスクリプトをアタッチする行為は、システム的には「そのオブジェクトにそのクラス型のコンポーネントを追加 (AddComponent) する」ことと全く同じ意味である。
* **型で検索するということ**
  * `GetComponent<型>()` や `FindAnyObjectByType<型>()` などの処理で「型」を探すということは、エディタ視点で見れば「そのスクリプトがアタッチされているオブジェクトを探す」ことと完全に同義になる。

### 2. 変数のアクセス修飾子とInspectorへの表示
Unityのスクリプトでは、変数をInspectorウィンドウに表示して、エディタ上から直接数値を調整したり、オブジェクトを割り当て（アサイン）したりできる。
* **public 変数**
    public float speed = 12f;
  * **特徴:** Inspectorに自動的に表示される。また、他のスクリプトからも自由にアクセス・変更ができる。
  * **メリット:** 記述がシンプルで、初心者向けのチュートリアルでよく使われる。
  * **デメリット:** どこからでも数値を書き換えられてしまうため、プロジェクトが大きくなると予期せぬバグ（意図しないところから変数を変更される）の原因になりやすい。

* **[SerializeField] private 変数 (推奨)**
    [SerializeField] private float speed = 12f;
  * **特徴:** Inspectorには表示されるが、`private` なので他のスクリプトからは直接アクセスできない。
  * **メリット:** 「エディタ上で設定はしたいが、プログラム上は他のスクリプトから勝手に書き換えられたくない」という場合に最適。クラスの独立性（カプセル化）を保てる。
  * **使い分けの実践:** モダンなゲーム開発では、安全性を高めるために「原則として変数は private にし、Inspectorで設定したいものにだけ [SerializeField] をつける」のが主流・ベストプラクティスとされています。

### 3. 主要なイベント関数 (ライフサイクル)
Unityには、特定のタイミングで自動的に呼ばれるメソッド（関数）が用意されています。用途に合わせて使い分けることが重要です。
* **Awake()**
  * そのオブジェクト（インスタンス）が生成・読み込まれた直後に、一生に1回だけ呼ばれる。
  * シーンに最初から配置されているものはゲーム開始時に、`Instantiate` 等でゲーム途中に生成されたものは「生成されたその瞬間」に呼ばれる。主に、自身のコンポーネントの取得（GetComponentなど）や絶対に必要な初期化処理に使われる。
* **Start()**
  * `Awake` の後、そのオブジェクトが初めて `Update`（毎フレーム処理）を迎える直前に1回だけ呼ばれる。
  * 他のオブジェクトへのアクセスや、生成時のステータス設定などに使われる。
* **Update()**
  * 毎フレーム（1秒間に数十回、PCのスペックによって変動）呼ばれる。
  * プレイヤーの入力（キーボードやコントローラー）の受け取りや、単純なタイマー処理などに使われる。
  * **文字列結合による負荷 (GC Alloc) に注意:** `"HP: " + hp` のような `+` による文字列結合をUpdate内で毎フレーム行うと、その都度メモリにゴミ (Garbage) が発生し、ゲームがカクつく原因になる。UIのテキスト更新などは「値が変化した時だけ」行うのがベストプラクティス。
* **FixedUpdate()**
  * 一定の時間間隔（デフォルトでは0.02秒ごと）で固定で呼ばれる。
  * **【重要】 Rigidbodyを使った物理演算（移動や力の加算など）は、必ずこの `FixedUpdate` 内で行う。**（Updateで行うと、フレームレートの変動によって動きがカクついたり不安定になるため）。
* **OnEnable() / OnDisable()**
  * オブジェクト（またはコンポーネント）が「有効化 (アクティブ)」になった瞬間、および「無効化」された瞬間に呼ばれる。初期設定やイベントの登録・解除によく使われる。
* **OnTriggerEnter(Collider other)**
  * 自身のCollider、または衝突相手のColliderの `Is Trigger` がONになっている時、物体同士が重なった（すり抜けて接触した）瞬間に呼ばれる。アイテムの取得や弾のヒット判定の基本。

### 4. 参照渡し (refキーワード)
C#において、通常メソッドに引数を渡す際は「値渡し (値のコピー)」となるため、メソッド内でその引数の数値を変更しても元の変数には影響しない。
* **ref の役割:** `ref` キーワードをつけることで「参照渡し (メモリ上の場所を渡す)」となり、メソッド内での値の変更が呼び出し元の変数にも直接反映される。
* **使い方:**
  * メソッド定義側: `void UpdateSpeed(ref float currentSpeed)`
  * 呼び出し側: `UpdateSpeed(ref speed);`
* **注意点:** `ref` で渡す変数は、メソッドに渡す前に必ず何らかの数値で初期化されている必要がある。

### 5. 主要なクラスと構造体
UnityのC#スクリプトで頻繁に使用される、ゲーム開発に必須の組み込み型。
* **Mathf (数学クラス)**
  * 算術計算に特化した静的 (static) クラス。ゲーム内でよく使われる計算メソッドがまとまっている。
  * 例: `Mathf.Abs` (絶対値)、`Mathf.Clamp` (値の上限・下限の制限)、`Mathf.Lerp` (2点間の線形補間など、滑らかな変化の計算)。
  * **float の == 比較は厳禁:** 浮動小数点 (`float`) は計算の過程で微小な誤差が生じるため、`if (timer == 1.0f)` のような厳密な一致判定はバグの原因になる。必ず `Mathf.Approximately(a, b)` を使用して「ほぼ等しいか」を判定する。
* **Physics (物理クラス)**
  * 物理演算を扱う静的クラス。`Physics.Raycast` (目に見えない光線[レイ]を飛ばし、何かに当たったかどうかの判定や、当たったオブジェクトの情報を取得する。銃の射撃や接地判定で頻出) などが代表的。
* **Vector3 (構造体)**
  * 3D空間の座標 (X, Y, Z) や、方向、速度などを表すためのデータ型。クラスではなく「構造体 (Struct)」であるため値型として扱われる。
  * よく使う定数が用意されており、`Vector3.zero` は `(0, 0, 0)`、`Vector3.up` は `(0, 1, 0)` などを表す。
  * **構造体と new の関係:** 値型であるため `Vector3 pos;` と宣言するだけでメモリ領域は確保されるが、中身は未定義（エラー）になる。そのため、Unityでは事実上ほぼ必ず `new Vector3(x, y, z)` と書き、コンストラクタで一気に初期化して安全な状態にする。
* **Transform (クラス)**
  * 全てのGameObjectに必ずアタッチされている基本のコンポーネント（クラス）。
  * オブジェクトの「位置 (Position)」「回転 (Rotation)」「拡縮 (Scale)」の情報を保持し、それらを操作するためのメソッドを提供する。また、親オブジェクト・子オブジェクトといった階層構造（親子関係）の管理も担う。
  * **方向ベクトルプロパティ (`forward`, `up`, `right`):**
    * 現在の「回転 (Rotation)」を基準にして、そのオブジェクト自身の向いている方向を簡単に取得できる機能。すべて**単位ベクトル（長さが1）**で返される。
    * `Transform.forward`: オブジェクトの「正面」（Z軸プラス方向）。弾を撃ち出す方向などに使う。
    * `Transform.up`: オブジェクトの「上」（Y軸プラス方向）。ジャンプの方向などに使う。
    * `Transform.right`: オブジェクトの「右」（X軸プラス方向）。
  * **position と localPosition の違い:** `position` はワールド空間（世界）の原点からの絶対座標。`localPosition` は親オブジェクトからの相対座標。UIや親に追従する子オブジェクトを動かす場合は、原則 `localPosition` を使わないと意図しない場所へ飛んでいってしまう。
  * **【超頻出の罠】 transform.position.x は直接書き換えられない:** `position` は変数ではなくプロパティであり、呼び出すと内部で Vector3 の「コピー」が返される。そのため `transform.position.x = 5.0f;` はコピーのX座標を書き換えるだけで本体に影響せず、コンパイルエラーになる。必ず `transform.position = new Vector3(5.0f, transform.position.y, transform.position.z);` のように新しい Vector3 を作って丸ごと代入し直す必要がある。
* **Vector2 (構造体)**
  * 2D空間の座標やベクトル（X, Y）を扱う。UIの位置やサイズ調整（RectTransform）などでも頻繁に使用される。
* **Color (構造体)**
  * マテリアルやUIの色（RGBA）を「0.0f 〜 1.0f」の範囲で扱うデータ型。`Color.red` や `Color.white` などの便利な定数があらかじめ用意されている。
* **Ray / RaycastHit (構造体)**
  * **Ray:** 発射位置 (`origin`) と方向 (`direction`) を持つ「光線」の定義。
  * **RaycastHit:** Rayが何かに衝突した際の詳細データ（当たった座標、対象のオブジェクトなど）を格納するための受け皿。
* **WaitForSeconds (クラス)**
  * コルーチン専用の待機用クラス。「指定した秒数だけ処理を待機させる」ために使用する。
  * `yield return new WaitForSeconds(3f);` のように `new` でインスタンス化して使う。
* **TextMeshProUGUI (クラス)**
  * UnityでUIとして文字を表示する際の現在のスタンダードクラス（古い `Text` クラスの上位互換）。
  * スクリプトから扱う場合は、ファイル冒頭に `using TMPro;` の記述が必要になる。

### 6. ビット演算子 (~0) と NavMesh.AllAreas
UnityのNavMeshや物理演算（Raycast）において、対象とするエリアやレイヤーは「ビットマスク（2進数の各桁をフラグに見立てたもの）」で管理される。
* **`~0` の意味:** `~` はビットを反転させる演算子。`0`（すべてのビットが0）を反転させると「すべてのビットが1」になる。
* **実用例:** `NavMesh.CalculatePath` などの引数で `~0` を渡すことは、「すべてのエリア（全ビットが1のフラグ）」を指定することと同義になる。これはUnityで用意されている `NavMesh.AllAreas` という定数を渡すのと全く同じ意味・挙動である。

### 7. out パラメータとインライン宣言 (out var)
メソッド内で計算した結果を、戻り値（return）とは別に引数経由で受け取るために `out` キーワードが使われる（例: Raycastの衝突情報 `hit` など）。
* **インライン宣言:** 従来はメソッドを呼ぶ前に受け皿となる変数（例: `RaycastHit hit;`）を事前に宣言しておく必要があったが、モダンなC#ではメソッドの引数部分に直接 `out var hit` と記述することで、変数の宣言と結果の受け取りを同時に行える。コードが短くスッキリするため頻繁に利用される。

### 8. コルーチン (IEnumerator と StartCoroutine)
通常のメソッドは呼び出されると1フレーム内で最後まで即座に実行されるが、コルーチンを使用すると「数秒待つ」「別の処理が終わるまで待機する」といった時間軸を伴う処理を複数フレームにわたって実行・一時中断できる。
* **IEnumerator**
  * コルーチンとして動作させるメソッドの戻り値に指定する型。内部で `yield return` を使用することで、処理の一時停止と再開が可能になる。
* **StartCoroutine()**
  * コルーチン処理を開始するための専用のメソッド。通常のメソッドのように直接呼ぶのではなく、`StartCoroutine(メソッド名())` の形で呼び出す必要がある。
* **yield return StartCoroutine(...)**
  * コルーチン内部でさらに別のコルーチンを呼び出し、「その処理が完全に終わるまで、現在の処理を待機させる」ための記述。
  * 例: 開始演出が終わるまで待ち、次にゲーム進行処理を実行し、終われば勝敗判定処理を実行する、といったゲームループ（順序立てた状態遷移）を直感的に記述できる。
* **yield return null;**
  * 処理を一旦ストップし、「次のフレーム（画面更新）まで待機する」ための記述。
  * `while (!条件) { yield return null; }` のように書くことで、ゲーム全体をフリーズさせずに、条件を満たすまで毎フレーム監視（待機）し続ける処理が安全に実装できる。

### 9. オブジェクトの生成 (Instantiate) と 削除 (Destroy)
* **Instantiate:** `Instantiate(プレハブ, 位置, 回転)` で、実行時に新しいオブジェクトをクローンして生み出す。
* **Destroy:** `Destroy(gameObject)` で、指定したオブジェクトをメモリ上から完全に削除する。第2引数に `Destroy(gameObject, 2.0f)` のように秒数を指定すると、時間差で消滅させることも可能。

### 10. Inspector拡張属性 (Attributes)
変数の上に記述することで、Inspectorの表示を整理・拡張できる機能。
* **[Header("文字列")]**: Inspector上に太字の見出し（カテゴリ名）を表示する。例: `[Header("Damage Reduction")]`
* **[Tooltip("文字列")]**: Inspectorで変数にマウスカーソルを合わせた際、説明文（ツールチップ）を表示させる。チーム開発で変数の意図を伝えるのに役立つ。
* **[FormerlySerializedAs("変更前の変数名")]**: 
  * 変数名を変更した際、Inspectorやプレハブに保存されていた過去の設定データがリセットされて消えるのを防ぐ属性。
  * 「以前はこの変数名だった」とUnityに教えることでデータを安全に引き継げる。※使用にはスクリプト上部に `using UnityEngine.Serialization;` が必要。
* **[HideInInspector]**: 
  * `public` な変数であっても、Inspector上には表示させないようにする属性。
  * 「他のスクリプトからはアクセスさせたいが、エディタ上からは勝手に数値を変更させたくない（内部処理専用）」という場合に使用する。
* **[Range(最小値, 最大値)]**:
  * floatやintの変数を、Inspector上で直接数値を入力するのではなく「スライダー」で直感的に調整できるようにする属性。例: `[Range(0f, 100f)]`
* **[Space(数値)]**:
  * Inspectorの表示において、変数の上に指定したピクセル分の「余白（隙間）」を空ける。項目が密集して見づらい時のUI整理に役立つ。例: `[Space(10)]`

### 11. C#の主要な構文
* **namespace (名前空間) と using 宣言:**
  * クラスをまとめる「グループ名」。異なる名前空間であれば、同じ名前のクラスが存在しても衝突しない。
  * **同じ名前空間の場合:** 別ファイル（別スクリプト）に書かれたクラスであっても、何も宣言せずにそのまま名前だけで呼び出して使える（C++の `#include` のような操作は不要）。
  * **違う名前空間の場合:** 他のグループにあるクラスを使いたい時は、**スクリプトファイルの一番上**に `using 違う名前空間名;`と書いてそのグループを読み込む必要がある。
* **foreach:** 配列やリストの中身を最初から最後まで順番に取り出すためのループ構文。`for`文より短く直感的に書ける。
* **var (暗黙的型推論):**
  * 右辺の代入値やメソッドの戻り値から「型」が明らかな場合、左辺の型の記述を省略できるキーワード。C++の `auto` に相当する。
* **型キャスト (`as` と `()`):**
  * C#で型の変換（キャスト）を行う際の2つの基本的な構文。
  * **`as` キャスト (安全):** `変数 as 型` と記述する。変換に失敗した場合はエラーにならず、安全に `null` を返す。（※C++の `dynamic_cast` に相当）
  * **`()` キャスト (強制):** `(型)変数` と記述する。変換に失敗すると例外エラーが発生して処理が停止する。
  * ※近年は `Instantiate<GameObject>(...)` のように、関数呼び出し時にジェネリクス（`<T>`）を使って直接型を指定し、キャスト自体を省略する書き方も主流。
* **`this` キーワードと参照の受け渡し:**
  * クラスの内部で「今実行されている自分自身のインスタンス（実体）」を指すキーワード。
  * **自身の参照渡し:** 別のオブジェクトを生成・初期化する際、`child.Setup(this);` のように「自分自身の参照」を引数として直接渡す設計パターンが頻出する。
  * **検索処理の回避:** 親玉（マネージャー等）から `this` を直接渡してあげることで、受け取った側は `FindAnyObjectByType` などの負荷の高い検索処理を行うことなく、安全かつ確実に親のメソッドやデータへアクセスできるようになる。

### 12. 高度な設計手法 (ScriptableObject, interface)
* **ScriptableObject:** 大量のデータ（敵のステータス一覧やアイテム図鑑など）をシーンのオブジェクトに持たせるのではなく、Projectウィンドウ上の「データファイル」として独立して保存するための特殊なクラス。メモリの節約に直結する。
* **interface (インターフェース):** 複数の異なるクラスに「共通のメソッド」を持たせるための契約（ルール）。例えば「ダメージを受ける」という `IDamageable` インターフェースを作れば、プレイヤーも敵も木箱も、同じメソッドでダメージ処理を呼び出せるようになる。

### 13. Time.deltaTime とフレームレート非依存の処理
* **なぜ必要か:** `Update()` が1秒間に何回呼ばれるかは、実行環境のスペックによって変動する（60fpsのPCと30fpsのスマホでは頻度が2倍違う）。もし `position += speed;` のように毎フレーム固定値を足すと、性能の高い端末ほどオブジェクトが速く動いてしまう。
* **Time.deltaTime:** 「前のフレームから今のフレームまでにかかった時間（秒）」を返すプロパティ。移動量に `Time.deltaTime` を掛けることで、`速度(1秒あたりの移動量) × 経過時間 = そのフレームでの移動量` となり、フレームレートに関わらず同じ速さで動くようになる。
  * 例: `transform.position += speed * Time.deltaTime * direction;`
* **注意:** `FixedUpdate()` 内では代わりに `Time.fixedDeltaTime` を使うのが基本（値は固定なので実質どちらでも大差ないが、意味的に区別される）。

### 14. 入力の取得 (Input クラス)
* **Input.GetAxis("Horizontal" / "Vertical")**
  * 矢印キーやWASD、ゲームパッドのスティックの入力を `-1.0 〜 1.0` の滑らかな値として取得する。徐々に加速・減速する自然な入力感が得られる。
* **Input.GetKeyDown(KeyCode.Space) / Input.GetKey(...) / Input.GetKeyUp(...)**
  * `GetKeyDown` はキーを押した「その瞬間の1フレームだけ」true、`GetKey` は押している間ずっとtrue、`GetKeyUp` は離した瞬間だけtrueになる。ジャンプなど「1回だけ実行したい処理」には `GetKeyDown` を使うのが基本。
* **Input.GetButtonDown("Fire1") など**
  * `Edit > Project Settings > Input Manager` で定義された抽象的な入力名を使う方式。キーボードとゲームパッドなど複数のデバイスに同じコード一つで対応できる。

### 15. コンポーネントの取得・追加 (GetComponent / AddComponent)
* **本体 (GameObject) と 部品 (Component) の違い**
  * **GameObject:** ヒエラルキーに存在する「オブジェクト本体（空の箱）」。Tankチュートリアルであれば「Tank（戦車）」そのもの。
  * **Component (スクリプト):** 本体にアタッチ（追加）される「部品」。
  * スクリプト内で `this` と書くと「コンポーネント（部品）自身」を指し、小文字の `gameObject` と書くと「自分がアタッチされている大元の本体」を指す。
* **GetComponent<T>()**
  * 自分自身、または他のGameObjectにアタッチされているコンポーネントへの参照を取得するメソッド。例: `Rigidbody rb = GetComponent<Rigidbody>();`
  * ※自身から取得する場合、本来は `gameObject.GetComponent<T>()` だが `gameObject.` は省略して記述できる仕様になっている。
  * 頻繁に呼び出す処理の場合、`Update()` の中で毎回呼ぶと負荷がかかるため、`Awake()` や `Start()` で一度取得してフィールド（変数）にキャッシュしておくのが定石。
* **AddComponent<T>()**
  * スクリプトから動的にコンポーネントを追加する。例: `gameObject.AddComponent<Rigidbody>();`
  * ※コンポーネント（部品）に対して別のコンポーネントを追加することはできないため、必ず「本体に対して (`gameObject.`)」追加すると明示して呼び出す必要がある。
* **GetComponentInChildren<T>() / GetComponentInParent<T>()**
  * 自分自身だけでなく、子オブジェクトや親オブジェクトの階層までさかのぼってコンポーネントを検索する派生メソッド。
* **シーン全体からの検索 (FindAnyObjectByType / GameObject.Find)**
  * **FindAnyObjectByType<T>()**: シーン上に存在するすべてのオブジェクトを検索し、指定したコンポーネント(型)を持つものを1つ取得する。
    * ※デフォルトでは「非アクティブ」なオブジェクトは検索から除外される。非アクティブなものも含めたい場合は `FindAnyObjectByType<型>(FindObjectsInactive.Include)` と引数を指定する。
  * **GameObject.Find("オブジェクト名")**: 名前（文字列）でシーン全体からオブジェクトを検索する。
  * **【重要】検索処理の重さ**: シーン全体を舐め回す検索処理は非常に負荷が高い（重い）。そのため `Update()` 内で毎フレーム呼ぶのは厳禁であり、必ず `Awake()` や `Start()` で一度だけ検索し、変数にキャッシュ（保存）して使い回すのが基本。

### 16. 衝突判定の使い分けとタグ比較
* **OnCollisionEnter と OnTriggerEnter の違い**
  * **OnCollisionEnter(Collision):** 両方のオブジェクトが `Is Trigger` オフの通常Colliderで、物理的にぶつかり合う（すり抜けない）場合に呼ばれる。壁や地面など「実体として押し返す」当たり判定に使う。
  * **OnTriggerEnter(Collider):** 片方以上の `Is Trigger` がオンの場合に呼ばれる。物理的な押し返しは発生せず、すり抜けながら「重なったこと」だけを検知する。アイテム取得やダメージ判定など「すり抜けさせたいが接触は検知したい」場合に使う。
* **【重要】 どちらも Rigidbody が必須**
  * `OnCollisionEnter` / `OnTriggerEnter` はいずれも、接触する2つのオブジェクトの**少なくとも片方に `Rigidbody` がアタッチされていないと呼ばれない**。「Colliderは付けたのにイベントが発火しない」という初心者のつまずきの多くは、このRigidbodyの付け忘れが原因。
* **CompareTag("Player") によるタグ比較**
  * `other.tag == "Player"` のような文字列比較でもタグを判定できるが、文字列比較は処理が重く、タイプミスにも気づきにくい。`other.CompareTag("Player")` を使うと高速かつ安全にタグを比較できるため、こちらが推奨される。

### 17. 回転の基本 (Quaternion)
* **なぜオイラー角 (X, Y, Z) をそのまま保存しないのか**
  * Unity内部では回転を `Quaternion`（クォータニオン）という4つの数値の組で管理している。オイラー角と違い「ジンバルロック」と呼ばれる特定角度で軸がおかしくなる現象が起きにくく、回転の補間（滑らかな変化）も計算しやすいため。
* **よく使うメソッド**
  * `Quaternion.Euler(x, y, z)`: 人間にわかりやすい角度（度数法）からQuaternionを作成する。
  * `transform.Rotate(Vector3)`: 現在の向きを基準に相対的に回転させる。
  * `Quaternion.LookRotation(方向ベクトル)`: 指定した方向を向く回転を作成する。敵が自機の方を向く処理などに使われる。
  * `Quaternion.Slerp(開始, 終了, 割合)`: 2つの回転の間を滑らかに補間する（`Vector3.Lerp` の回転版）。

### 18. 配列と List<T>
* **配列 (Array):** 
  * `int[] scores = new int[5];` のように、要素数が固定のデータの集まり。宣言時に決めたサイズは後から変更できない。
  * **実体はクラス (System.Array):** C#の配列は単なるメモリの連続領域ではなく、内部的にはすべて `System.Array` というクラスを継承して作られたオブジェクト（参照型）である。そのため、`scores.Length` のように要素数を取得するプロパティなどを最初から備えている。
* **List<T>:** `using System.Collections.Generic;` を使い `List<int> scores = new List<int>();` のように宣言する、要素数を自由に増減できる可変長のリスト。`Add()` で追加、`Remove()` で削除、`Count` で現在の要素数を取得できる。
* **使い分け:** 出現する敵の数や取得したアイテムの数など、実行中に増減するデータを扱う場合は `List<T>` が便利。要素数が最初から決まっている場合は配列でも問題ない。

### 19. デバッグの基本 (Debug.Log)
* **Debug.Log(内容) / Debug.LogWarning(内容) / Debug.LogError(内容)**
  * Consoleウィンドウに任意のメッセージを出力するメソッド。変数の中身やメソッドが呼ばれたタイミングを確認するのに使う、最も基本的で強力なデバッグ手段。
  * 例: `Debug.Log("現在のHP: " + currentHealth);` のように変数を文字列と連結して出力すると状況把握がしやすい。
* **Debug.LogとConsoleウィンドウの連携**
  * 出力したログをダブルクリックすると、そのログを呼び出したスクリプトの行にジャンプできるため、「どのコードがいつ実行されたか」を追跡するのに便利。

### 20. 基本的な制御構文 (if / for / while / switch)
* **if / else if / else:** 条件によって処理を分岐させる最も基本的な構文。`if (health <= 0) { Die(); }` のように条件式の結果（true/false）で処理を選択する。
* **for文:** 「決まった回数だけ繰り返す」処理に使う。`for (int i = 0; i < 10; i++) { ... }` のように、初期化・継続条件・更新式の3つを指定する。
* **while文:** 「ある条件が満たされている間ずっと繰り返す」処理に使う。回数が事前に決まっていないループに向く。
* **switch文:**
  * 1つの変数の値によって多数の分岐を行う場合、`if / else if` の連続よりも見通しよく書ける構文。
  * `enum`（列挙型）と非常に相性が良く、状態に応じて処理を切り替える「ステートマシン」の土台として頻出する。
  * **【C++との違いと安全設計】:** C#では、各 `case` の処理の最後には**必ず `break;` を書く義務がある**（忘れるとエラーになる）。C++のような「break忘れによる処理の貫通（フォールスルー）」が起きないため、非常に安全に使える。
  * 例:
    ```csharp
    switch (currentState)
    {
        case GameState.MainMenu:
            ShowMenu();
            break; // 必須
        case GameState.Game:
            GameStart();
            break; // 必須
    }
    ```

### 21. 画面座標からワールド座標への変換 (3Dと2Dのアプローチの違い)
* **【基本】3Dゲームでの座標取得 (Perspectiveカメラ / Raycast方式)**
  * 遠近感がある `Perspective`（透視投影）カメラでは、画面上の1点に対応する3D空間の座標が、奥行き (Z) によって大きく変化する。
  * そのため、**`Physics.Raycast(ray, out hit)`** を使って座標を確定させるのが標準的な手法。
  * **【重要】Colliderの必須性:** レイを受け止める側（地面やオブジェクト）に必ず **`Collider`** がアタッチされている必要がある（付いていないとレイが通り抜けてしまいヒットしない）。
  * **引数と out キーワードの仕組み:**
    * `ray`（飛ばすレーザー）を放ち、何かに当たったかどうかを `true` / `false` で返す。
    * 第2引数の `out hit` は結果を入れる「受け皿」。「当たったらその受け皿に結果を詰めてね」という命令であり、ヒットした場合は対象の座標 (`hit.point`) などの詳細情報がこの変数に自動的に書き込まれる。

* **【補足】2Dゲームでの座標取得 (Orthographicカメラ)**
  * 2Dゲーム標準の `Orthographic`（平行投影）カメラでは遠近感がなく、距離に関わらずオブジェクトが同じ大きさで表示される。
  * 内部的な奥行き(Z)をどう固定しようと画面に映る見た目は変化しないため、以下の手法で座標を取得できる。
    * **方法A (2D Raycast):** `Camera.main.ScreenToWorldPoint` で2D座標を取り、**`Physics2D.Raycast(worldPoint, Vector2.zero)`** でその点に何があるか判定する（対象に `Collider2D` が必要）。
    * **方法B (固定Z値):** **`Camera.main.ScreenToWorldPoint(new Vector3(x, y, Camera.main.nearClipPlane + 10f))`** のように引数に固定のZ値を渡し、Colliderに頼らず計算のみでワールド座標を確定させる。

### 22. 列挙型 (enum) と状態管理
* **enum（列挙型）とは**
  * 複数の「状態」や「種類」に対して、人間が分かりやすい名前（ラベル）をつけてグループ化したもの。内部的には自動的に `0, 1, 2...` という連番の整数として割り当てられる。
* **状態管理での活用（メリット）**
  * 例: `public enum GameState { MainMenu, Game }`
  * 上記のように宣言すると、「このゲームには `MainMenu`（0）と `Game`（1）という2つの状態がある」という専用の型を独自に定義したことになる。
  * 条件分岐を書く際、`if (currentState == 0)` のように数字の暗号で書くよりも、`if (currentState == GameState.MainMenu)` と書く方が「タイトル画面の時」という意図が一目で分かり、チーム開発や後からの見直しにおけるバグを大きく減らすことができる。
* **switch文との相性**
  * `enum` は `switch` 文と組み合わせて使われることが非常に多い。現在の状態（GameState）に応じて、実行する処理（プレイヤーの操作を受け付けるか、UIを表示するか等）を綺麗に切り替える「ステートマシン」の土台となる。

## 14. C# for C++ Developers (C++経験者向けのC#の主な違いと注意点)

### クラスとメモリ管理の違い
* **ヘッダファイルと #include がない**
  * C#には `.h` と `.cpp` の分離がなく、宣言と実装は常に同じ1つの `.cs` ファイル内に記述する。
  * また、C++の `#include` のようなファイル読み込みの仕組みは存在しない。Unityではプロジェクト内のすべての `.cs` ファイルが一括でコンパイルされるため、同じ名前空間 (namespace) に属している（またはグローバル空間にある）別ファイルの自作クラスは、何も宣言せずにそのまま直接呼び出して使うことができる。
* **アクセス修飾子の書き方**
  * C++の `public:`（ブロック単位）とは異なり、C#では `public int speed;` のようにすべての変数やメソッドの先頭に個別に修飾子を書く必要がある。
* **クラス型変数はすべて「ポインタ (参照型)」**
  * C#において `class` で定義された変数はすべて実体ではなくメモリのアドレスを指す「参照 (Reference)」である。C++のように `*` や `&` を意識して書く必要はないが、代入操作はすべて「ポインタの共有」になる点に注意。
* **コンストラクタには必ず `new` が必要**
  * インスタンスを生成する際は、ローカル変数であっても必ず `new` キーワードを使用してメモリを確保する。
* **配列の構文と振る舞い (C++の生配列や std::vector との違い)**
  * **宣言の書き方:** C++の `int arr[]` と異なり、C#では `int[] arr` のように `[]` を型名の直後にくっつけて書く（「int配列型」という独立した型として扱うため）。
  * **【重要】代入は「ポインタの共有」:** C++の `std::vector` などは `=` で代入すると中身の全コピー（ディープコピー）が走るが、C#の配列はクラス（参照型）であるため、`配列A = 配列B;` のように代入した場合は**「同じ配列データを指すポインタの共有（参照のコピー）」**になる。中身の複製は起きず、片方の要素を書き換えるともう片方にも影響する点に要注意。
* **null (C++の `nullptr` に相当)**
  * 参照型の変数（クラス、GameObject、文字列など）が、「メモリ上のどこも指していない（空っぽ）」状態を表すキーワード。
  * C++のNullポインタアクセスと同様、この `null` の状態のまま中身（メソッドやプロパティ）にアクセスしようとすると、Unity最頻出エラーである `NullReferenceException`（ぬるぽ）が発生して処理が止まる。
  * **【重要】** `int` や `float`、`struct`（`Vector3`など）といった「値型」は、宣言と同時にメモリが確保されるため、原則として `null` になることはない。

### struct (構造体) と class (クラス) の明確な分断
* C#における `struct` は「値型 (Value type)」であり、代入や関数の引数に渡すと必ず「中身の完全なコピー」が発生する（スタックメモリ）。対して `class` は「参照型 (Reference type)」である（ヒープメモリ）。
* **型のアクセス制御（修飾子）の決定的な違い:**
  * **C#:** 型の宣言前に `public` や `private` をつけて公開範囲を制御できる（例: `public struct Point`）。省略した場合、名前空間直下なら `internal`（プロジェクト内限定）、クラス内なら `private` がデフォルトになる（※このルールは struct / class 共に共通）。
  * **C++:** 型の宣言前に `public` などはつけられない（`public struct Point` と書くとコンパイルエラーになる）。C++でのアクセス修飾子は「型の中のメンバ」や「継承」に対してのみ使用する。なお、C++での `struct` と `class` の違いはメンバのデフォルトアクセスレベル（前者が `public`、後者が `private`）のみである。
* **new キーワードの扱いの違い (重要):** 
  * `class` (参照型) は `new` を使わないと実体（メモリ領域）が作られない。
  * `struct` (値型) は宣言した時点でスタック上に領域が確保されるため、`new` がなくても変数自体は作れる。
  * ただし、`struct` でも `new` を使えば「コンストラクタを呼んで一気に初期化する」ことができる（例: `Vector3` の初期化など）。
* **構造体の値渡しによる不要なコピーを避ける:** `Vector3` などは構造体であるため、関数に渡す際に無駄なコピーを避ける場合は `ref` キーワードを使うなどの工夫ができる。

### プロパティ (Getter / Setter の完全進化系)
C++で手書きしていた `GetSpeed()` / `SetSpeed(float v)` のようなカプセル化を、変数のような見た目で書ける機能。
* **正体は「変数ではなくメソッドのペア」**
  * `public float MoveSpeed { get; }` は変数の宣言に見えるが、実体は `get_MoveSpeed()` というメソッドで、コンパイル時に自動変換される。外部からは `obj.MoveSpeed` と変数のようにアクセスできるが、裏側では毎回メソッドが呼ばれている点がC++の素朴なpublic変数と異なる。
* **完全形（get + set）**
```csharp
  private float moveSpeed;  // 実体（バックアップ用のフィールド）。外から直接触らせたくない値

  public float MoveSpeed    // プロパティ本体（外部への公開窓口）
  {
      get { return moveSpeed; }   // 読み取り時（例: float x = obj.MoveSpeed;）に呼ばれ、moveSpeedの中身を返す
      set { moveSpeed = value; }  // 代入時（例: obj.MoveSpeed = 5.0f;）に呼ばれる。`value`には代入された値が自動で入る
  }
```
  * メリット：`set` 内で検証処理を挟める（例: `set { moveSpeed = Mathf.Max(0, value); }` として負の値を弾く）。ロジックを後から追加しても呼び出し側のコードは変更不要。
* **自動実装プロパティ（`{ get; set; }`）**
  * `get`/`set` の中身が単純な代入・返却だけなら、裏の変数を省略してこう書ける：`public float MoveSpeed { get; set; }`（裏の実体変数はコンパイラが自動生成）。Inspectorに表示したい、または代入時に検証処理を挟みたい場合は完全形を使う。
* **式形式プロパティ（`=>`）による読み取り専用の省略記法**
```csharp
  public float HealthRatio => currentHealth / maxHealth;
  public bool IsAlive => currentHealth > 0;
```
  * これは `get { return ...; }` だけを持つ完全形（例: `public float HealthRatio { get { return currentHealth / maxHealth; } }`）を1行に凝縮したもの。`=>` は「呼び出されたら右辺を実行して返す」という意味の矢印（ラムダ式と同じ記法）。
  * `set` は書けないため、外部から代入しようとするとコンパイルエラーになる。「他の値から計算して求める」「外から書き換えさせたくない」値を公開するのに向く。
  * メソッドとの違い：`HealthRatio()` のように `()` を付けず、変数と同じ感覚でアクセスできる点。